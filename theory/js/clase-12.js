
//ForEach

const colors = ['Red','Blue','Yellow'];

colors.forEach( color => {
    if( color === 'Blue'){
        console.log('Este es el rojo');
    } else {
        console.log(`No es el azul es ${color}`);
    }
});